<div class="topbar">
  <div class="topbar-inner">
    <div class="container-fluid">
      <%= link_to "Katalog miniatur", 'home', class: "brand" %>
      <ul class="nav">
        
        <% controller.action_methods.each do |item| -%>
          <%= content_tag(:li, link_to(item.capitalize, eval(item+"_path")), class: (controller.action_name == item ? "active" : "")) %>
        <% end -%>
      </ul>
      <p class="pull-right">zalogowany jako:<a href="#">username</a></p>      
    </div>
  </div>
</div>
